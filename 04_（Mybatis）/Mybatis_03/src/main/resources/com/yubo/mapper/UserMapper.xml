<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yubo.mapper.UserMapper">
    <select id="findAll" resultType="com.yubo.pojo.User">
        select * from user
    </select>

    <select id="findById" resultType="com.yubo.pojo.User">
        select * from user where id = #{id}
    </select>

    <insert id="insert">
        insert into user(id,username,password,name,age) values (null,#{username},#{password},#{name},#{age})
    </insert>

    <delete id="deleteById">
        delete from user where id = #{id}
    </delete>

    <update id="update">
        update user set username = #{username},password = #{password} , name = #{name} , age = #{age} where id = #{id}
    </update>

</mapper>